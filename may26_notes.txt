*Compiler is a language translator
 interpreters are language evaluators
*python performs negative indexing(right to left)
*data structure
	-mutable(modifiable) or immutable
	strings immutable,list mutable	
*dir(a)-lists methods that can be called on a
*python lists are heterogenous and mutable
*Data structures are lazily evaluated (consernes memory)
	strict eval-values are evaluated beforehand

*a=b---->a and b are ALIASes to the same variable
	REFERENCES pointing to the same lst 
 a=[1,2,3,4]		
			2000
			--------
			1 2 3 4
			--------
3000
------
2000
------
			5000(b)
			--------
			2000
			--------
